
# MovieBox — Vite + React + Tailwind demo

## How to run

1. Unzip the project folder.
2. Open a terminal in the project root.
3. Run `npm install`
4. Run `npm run dev`
5. Open the URL shown by Vite (usually http://localhost:5173/ )

This is a demo landing page with a signup modal (client-side only).
